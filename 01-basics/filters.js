const myArr=[1,2,3,4,5]

const Greaters=myArr.filter( (num) => {
    return num>3
})

//console.log(Greaters);

// #############  filter with for each ##############


myArr.forEach( (num) => {
    if(num > 2){

    }
});